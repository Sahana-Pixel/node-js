// const sub = (a,b) => a+b;
// const mul = (a,b) => a*b;

//2. this hvae to  be exported
// module.exports={sub,mul}

// 4.after changes dirctly expport is used
export const sub = (a,b) => a+b;
export const mul = (a,b) => a*b;